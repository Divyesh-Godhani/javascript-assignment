// 06. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

let comapany = 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon';

let groupofcomapany = comapany.split(", ");

console.log(groupofcomapany);